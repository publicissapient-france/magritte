# Scala API

## Build

```
sbt test # run tests
sbt docker:publishLocal # build image in local repository
```

Run in local mode :
* `export MODELS_PATH=???`
* Run class `MagritteAPI`

## RAF

- inclure une basic auth
